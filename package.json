{
  "name": "apogee-forge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A ten-battle solo fleet-forging game with hidden enemy dice and simultaneous counters.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check app.js && node --check battle-app.js && node --check game-engine.js && node --check fleet-game-engine.js && node --check battle-engine.js && node --check battle-content.js && node --check solo-battle-engine.js && node --check server.js && node --check fleet-engine.js && node --check fleet-dice.js && node --test",
    "simulate:battle": "node scripts/simulate-battle.mjs",
    "simulate:live": "node scripts/simulate-live-game.mjs",
    "test:fleet": "node --test test/fleet-engine.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
