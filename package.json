{
  "name": "apogee-forge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A ten-round solo dice-forging score attack.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check app.js && node --check game-engine.js && node --check server.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
